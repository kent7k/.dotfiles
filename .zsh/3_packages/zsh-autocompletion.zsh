# Deals with such a error that 2 characters couldn't disappear when zsh complement.

# Changes the color of suggestion.
ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE="fg=50"

# Initialises completions with ZSH's compinit
# autoload -Uz compinit && compinit
